# Lab28 package

Run this folder for the final submission layout.

```bash
cd lab28
cp ../.env.example .env
docker compose up -d --build
```

The root README contains the full runbook, rubric mapping, Kaggle cloudflared setup, and LangSmith setup notes.
